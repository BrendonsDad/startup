# Debugging Node.js
Previously your JS debugger was done by running the live server. You can debug using debugging tools built into VS Code. 

You can debug main.js in VS Code by executing the Start Debugging command by pressing F5